name: patientappointment
description: "a new flutter project for managing patient appointments"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2 
  intl: ^0.19.0 
  uuid: ^4.4.0 

  hive: ^2.2.3 
  hive_flutter: ^1.1.0 
  path_provider: ^2.1.3
  flutter_local_notifications: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

  hive_generator: ^2.0.1
  mockito: ^5.4.4
  build_runner: ^2.4.9

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/seed/